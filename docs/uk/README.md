# Weqi

**Weqi** — це сучасний, чистий застосунок для гри в шахи з відкритим кодом. Усі правила шахів реалізовані в локальному рушії C++, з підтримкою гри людина проти людини, людина проти ШІ, ШІ проти ШІ та перегляду партій.

> **Читайте цей документ своєю мовою:**
> [English](../../README.md) · [简体中文](../zh-CN/README.md) · [繁體中文](../zh-TW/README.md) · [日本語](../ja/README.md) · [Español](../es/README.md) · [한국어](../ko/README.md)

---

## Можливості

- **Чотири режими гри**
  - **Людина проти людини** — два гравці на одному пристрої.
  - **Людина проти ШІ** — гра проти обраного постачальника ШІ.
  - **ШІ проти ШІ** — два ШІ грають автоматично (не враховується у вашій статистиці).
  - **Перегляд партій** — перегляд ваших історичних партій.
- **Повні правила шахів**, реалізовані локально на C++ (перевірка легальних ходів, шах, мат, пат, рокірування, взяття на проході, перетворення пішака тощо).
- **Профіль гравця та рейтинг** — відстежуйте свій рейтинг, найкращий рейтинг і редагуйте ім'я гравця.
- **Статистика** — зіграні партії, відсоток перемог, перемоги, нічиї, поразки, найкраща/поточна серія перемог, найкращий рейтинг.
- **Історія партій** — перегляд минулих партій із датою, режимом, суперником, результатом і зміною рейтингу.
- **Продовження незавершеної партії** — відновлення поточної партії в будь-який час.
- **Постачальники ШІ** — додавання, редагування та видалення постачальників ШІ (ім'я, тип, базова URL-адреса, ключ API, модель). Ключі API зберігаються приватно в каталозі даних користувача системи, ніколи в проєкті.
- **Чат ШІ** — ШІ коротко коментує свій хід; повідомлення відповідає мові вашого інтерфейсу.
- **Інтернаціоналізація (i18n)** — 7 мов інтерфейсу: спрощена китайська, традиційна китайська, англійська, японська, іспанська, українська та корейська. Змінюйте мову під час роботи без перезапуску.

## Технологічний стек

- **C++17**
- **Qt 6** (Widgets)
- **CMake**

## Структура каталогів

```
weqi/
├── CMakeLists.txt
├── README.md
├── ai_adapter/          # Адаптер ШІ на Python (визначення мови, повторні спроби/навчання)
│   ├── main.py
│   ├── parser.py
│   └── providers/
│       └── openai_compatible.py
├── resources/           # Файли ресурсів Qt
├── scripts/             # Допоміжні скрипти
├── src/
│   ├── main.cpp         # Точка входу програми
│   ├── MainWindow.h/.cpp
│   ├── GameController.h/.cpp
│   ├── ChessBoard.h/.cpp
│   ├── ChessPiece.h/.cpp
│   ├── ai/              # Менеджер ШІ
│   ├── data/            # Менеджери налаштувань, мови, профілю, статистики та історії
│   └── pages/           # Головна, нова партія, партія, людина-ШІ, ШІ-ШІ, історія, налаштування, про застосунок
├── tests/               # Тести C++
├── tools/               # Інструменти розробки
└── translations/        # Файли перекладу Qt .ts (7 мов)
```

## Збірка

### Залежності

- CMake ≥ 3.16
- Qt 6 (≥ 6.2, включно з модулем Widgets)
- Компілятор із підтримкою C++17 (GCC / Clang)
- Python 3 (для адаптера ШІ)

### Кроки збірки

```bash
# 1. Налаштування
cmake -S . -B build

# 2. Збірка
cmake --build build -j

# 3. Запуск
./build/Weqi
```

Або одним рядком:

```bash
cmake -S . -B build && cmake --build build -j && ./build/Weqi
```

### Переклади

Файли перекладу знаходяться в `translations/` і компілюються в бінарний файл через `qt_add_translations` Qt. Після зміни рядків `tr()` оновіть переклади:

```bash
# Оновлення файлів .ts (витягнення нових рядків)
lupdate src -ts translations/weqi_<lang>.ts

# Випуск файлів .qm
lrelease translations/weqi_<lang>.ts
```

## Використання

- **Вибір фігури**: клацніть лівою кнопкою миші будь-яку фігуру, щоб виділити її.
- **Хід фігурою**: після вибору клацніть цільову клітинку. Локальний рушій перевіряє легальність ходу.
- **Скасування ходу**: використовуйте кнопку «Скасувати хід», щоб повернути хід назад.
- **Нова партія**: почніть нову партію зі сторінки гри або головного екрана.
- **ШІ проти ШІ**: оберіть двох постачальників ШІ та використовуйте «Почати / Пауза / Продовжити / Стоп» для керування автоматичною партією.
- **Зміна мови**: відкрийте «Налаштування → Мова» та оберіть одну з 7 підтримуваних мов (або «Слідувати за системою»).

## Зберігання даних

- **Налаштування та профіль**: зберігаються в каталозі даних користувача системи (наприклад, `~/.local/share/Weqi/` у Linux).
- **Конфігурація постачальників ШІ**: зберігається окремо в каталозі даних користувача, ніколи в проєкті.
- **Історія партій та збережені партії**: зберігаються в каталозі даних користувача.

## Ліцензія

Відкритий код. Деталі дивіться в репозиторії.
